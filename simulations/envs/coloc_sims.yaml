name: coloc_sims
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.12
  - pip
  - tskit
  - pyslim
  - msprime
  - tstrait
  - stdpopsim
  # Arrives transitively with stdpopsim, but human_simulation_o2.py
  # imports it directly to load demography/*.yaml -- declare it.
  - demes
  - hail
  - pandas
  - numpy
  - matplotlib
  - seaborn
  - bcftools
  - htslib
  - tabix
  - plink2
  - snakemake-minimal>=8
  - snakemake-executor-plugin-slurm
